# C2-031 Evidence

## Status

Current status: partial, SSH live-state probe exists with deterministic unit tests. Physical target-state proof remains.

## Implemented Evidence

- `android/domain/targets/src/main/kotlin/io/codecks/domain/targets/TargetModels.kt`
  - Defines `LiveState` values: Configured, Connecting, Online, Degraded, Offline, Auth Failed, Host Key Changed.
- `android/transport/ssh/src/main/kotlin/io/codecks/transport/ssh/SshTargetProbe.kt`
  - Probes trusted SSH targets with `printf codecks-ready`.
  - Maps successful command to `ONLINE`.
  - Maps auth failure, host-key mismatch, timeout, and connection failure to typed live states.
- `android/transport/ssh/src/test/kotlin/io/codecks/transport/ssh/SshTargetProbeTest.kt`
  - Proves success maps to `ONLINE`.
  - Proves auth failure maps to `AUTH_FAILED`.
  - Proves pre-known host-key-changed state returns `HOST_KEY_CHANGED` without network work.

## Remaining Gates

- Persist and observe live target state in app UI.
- Real Mac online/offline/auth/pin physical tests.
- Connection cancellation and retry/backoff policy.
- Distinguish degraded permissions from unreachable transport on a real host.
