# C2-026 Evidence

## Status

Current status: in progress.

Local release boundary checker exists to detect forbidden cloud, hosted backend, telemetry, login, billing, and commercial strings.

## Evidence

- `tools/check-local-release-boundary.sh`
- Script validates JSON syntax under `protocol/` and `mac-actions/` when `node` or `jq` is available.
- Script scans non-Android localRelease sources for forbidden strings.

## Remaining Gates

- Run checker in CI for localRelease.
- Tune false positives as product copy and platform code settle.
- Extend scan inputs once release packaging paths are finalized.
