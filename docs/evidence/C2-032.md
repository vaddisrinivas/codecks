# C2-032 Evidence

## Status

Current status: partial, real first-run SSH setup path is wired through app UI and transport seams. Physical clean-install timing proof remains.

## Implemented Evidence

- `android/app/src/main/java/io/codecks/app/ui/CodecksApp.kt`
  - Adds first-run screen with `Connect my Mac`, `Try the demo Deck`, and local/no-cloud copy.
  - Connect flow is task UI, not a drawer destination.
  - Seeds the default deck into Room and observes the persisted deck list for rendering.
  - Wires the connection setup route to Keystore public-key generation, public-key copy, one-time password install, manual key-installed fallback, and first Finder proof.
  - Wires Deck and Trackpad Keyboard entry points to a task-only Keyboard route that returns to its origin.
- `android/app/src/test/kotlin/io/codecks/app/ui/AppRouteTest.kt`
  - Proves Keyboard is a task route, not a drawer destination.
  - Proves Keyboard return routing preserves Deck/Trackpad origin and never loops to Keyboard.
- `android/feature/deck/src/test/kotlin/io/codecks/feature/deck/DeckScreenMappingTest.kt`
  - Proves default Deck keys have unique lazy-grid keys.
  - Proves local Trackpad and Keyboard actions route to task screens instead of generic action handling.
- `android/app/src/main/java/io/codecks/app/data/CodecksDataContainer.kt`
  - Creates app-local Room-backed deck and receipt repositories.
  - Provides `SshjPublicKeyInstaller`, `AndroidKeystoreSshCredentialProvider`, and SSH-backed first Finder proof use case.
- `android/feature/connection/src/main/java/io/codecks/feature/connection/ConnectionSetupScreen.kt`
  - Maps setup draft into checklist, trust/key/test gates, username/address/fingerprint fields.
  - Shows generated phone public key, copy/manual-install controls, password-assisted install action, and first Finder proof result.
- `android/domain/targets/src/main/kotlin/io/codecks/domain/targets/TargetSetupPolicy.kt`
  - Adds setup-draft conversion to a trusted SSH target so install can happen before `keyInstalled` is true.
- `android/transport/ssh/src/main/kotlin/io/codecks/transport/ssh/SshPublicKeyInstaller.kt`
  - Implements the real one-time public-key install transport.
- `android/runtime/actions/src/main/kotlin/io/codecks/runtime/actions/RunFirstFinderProofUseCase.kt`
  - Plans `mac.finder.open` through shared action planner and executes via a replaceable executor.
- `android/domain/actions/src/main/kotlin/io/codecks/domain/actions/ActionExecutionPorts.kt`
  - Defines `MacActionExecutor` as the executor port consumed by the first Finder proof.
- `android/transport/ssh/src/main/kotlin/io/codecks/transport/ssh/SshMacActionExecutor.kt`
  - Implements the executor port through SSH command transport.
  - Maps `mac.finder.open` to `open -a Finder`.
- `android/app/src/main/java/io/codecks/app/data/CodecksDataContainer.kt`
  - Wires the app's first Finder proof use case to the SSH executor seam.
- `android/runtime/actions/src/test/kotlin/io/codecks/runtime/actions/RunFirstFinderProofUseCaseTest.kt`
  - Verifies success receipt for first Finder proof.
- Verification run:
  - `ANDROID_HOME=$ANDROID_HOME ./gradlew assembleLocalDebug assembleIncubatorDebug :app:testLocalDebugUnitTest :feature:deck:testDebugUnitTest :domain:targets:test :domain:decks:test :runtime:actions:test :data:decks:testDebugUnitTest :data:receipts:testDebugUnitTest :data:targets:testDebugUnitTest :core:security:test :transport:ssh:test checkReleaseBoundary`
  - `./tools/check-local-release-boundary.sh`

## Remaining Gates

- Physical clean-install under-five-minute proof.
- Persisted target selection.
- Process-death restore proof on device.
