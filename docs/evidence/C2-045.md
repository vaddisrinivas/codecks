# C2-045 Evidence

## Status

Current status: partial, strict JSON codec proof.

## Implemented Evidence

- `DeckJsonCodec.export()` validates before export.
- `DeckJsonCodec.import()` rejects unknown JSON fields, unsupported versions, invalid topology, duplicate IDs, unknown action IDs, and overlap.
- `DeckJsonCodecTest` covers valid round-trip, invalid topology, and quarantine path.

## Remaining Gates

- User-facing import/export flow.
- File picker and export destination UX.
- Secret-scan of exported payloads.
- Legacy import mapping and reviewed quarantine screen.

