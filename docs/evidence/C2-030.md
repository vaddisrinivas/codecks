# C2-030 Evidence

## Status

Current status: partial, automated state-machine proof only.

## Implemented Evidence

- `android/domain/targets/src/main/kotlin/io/codecks/domain/targets/TargetModels.kt`
  - `TargetSetupDraft.trustFingerprint()` requires host, username, port, and fingerprint.
  - `TargetSetupDraft.installPhoneKey()` requires trusted fingerprint first.
  - `TargetSetupDraft.toMacTarget()` emits `TrustState.TRUSTED` only after trust and key install gates.
- `android/domain/targets/src/test/kotlin/io/codecks/domain/targets/TargetSetupPolicyTest.kt`
  - Verifies trust cannot happen before address.
  - Verifies key install and Finder proof ordering.

## Remaining Gates

- Real SSH host-key retrieval from Android.
- Fingerprint comparison UI backed by live transport.
- Host-key changed/rotation/mismatch paths.
- Physical Mac evidence.

