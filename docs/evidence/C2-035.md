# C2-035 Evidence

## Status

Current status: partial, Room-backed durable deck store implemented. Migration and process-death proof remain.

## Implemented Evidence

- `android/domain/decks/src/main/kotlin/io/codecks/domain/decks/DeckModels.kt`
  - Defines `DeckRepository` port and import result contract.
- `android/data/decks/src/main/kotlin/io/codecks/data/decks/DeckJsonCodec.kt`
  - Validates deck version, page/button id uniqueness, grid bounds, button overlap, span range, required labels, and stable action IDs.
  - Provides in-memory repository with quarantine store.
- `android/data/decks/src/main/kotlin/io/codecks/data/decks/RoomDeckStore.kt`
  - Adds Room entities for decks and quarantined imports.
  - Adds `DeckDao`, `DeckDatabase`, and `RoomDeckRepository`.
  - Stores canonical strict JSON, sorts observed decks, and quarantines failed imports.
- `android/data/decks/src/test/kotlin/io/codecks/data/decks/DeckJsonCodecTest.kt`
  - Proves default deck round-trip.
  - Proves overlap/out-of-bounds decks are quarantined.
  - Proves repository stores valid deck and quarantines invalid JSON.
- `android/data/decks/src/test/kotlin/io/codecks/data/decks/RoomDeckStoreTest.kt`
  - Proves Room entity mapping preserves strict deck JSON and quarantine metadata.
- `android/app/src/main/java/io/codecks/app/data/CodecksDataContainer.kt`
  - Wires app-local Room databases for decks and receipts.
- `android/app/src/main/java/io/codecks/app/ui/CodecksApp.kt`
  - Seeds the default deck into the Room-backed repository and renders observed persisted decks.

## Remaining Gates

- Migrations beyond schema version 1.
- Corrupt on-disk data quarantine.
- Process-death restoration evidence.
- Import/export UI and user review screen.
